import 'package:flutter/material.dart';

const logo = Icon(Icons.message);

//Screen Name
const splash = "/splash";
const onBoard = "/onBoard";
const home = "/home";
const loginPage = "/loginPage";
const chatroom = "/chatroom";
