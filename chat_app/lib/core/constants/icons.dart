import 'package:flutter/material.dart';

final icApp = Icon(
  Icons.message_rounded,
  color: Colors.white,
  size: 170,
);
final icAppSmall = Icon(
  Icons.message_rounded,
  color: Colors.white,
  size: 80,
);

final icAccount = Icon(
  Icons.account_circle_rounded,
  color: Color(0xFF236BBD),
);

final icPass = Icon(
  Icons.lock,
  color: Color(0xFF236BBD),
);
